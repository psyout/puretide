# No environment variables needed for this privacy-focused shop
# All data is stored client-side only

# Wrike Integration (optional - for orders and stock alerts)
# WRIKE_API_TOKEN=your_wrike_api_token
# WRIKE_ORDERS_FOLDER_ID=your_orders_folder_id
# WRIKE_STOCK_ALERTS_FOLDER_ID=your_stock_alerts_folder_id

# Client data is stored in Google Sheets "Clients" tab (auto-created)

# DigiPay credit card payments (from DigiPay when you onboard)
# DIGIPAY_SITE_ID=your_site_id
# DIGIPAY_ENCRYPTION_KEY=your_encryption_key
# DIGIPAY_POSTBACK_URL=https://puretide.com/api/digipay/postback
# charge_amount is sent as decimal string (e.g. 29.99); CAD display is configured in your DigiPay gateway account
# DIGIPAY_POSTBACK_HMAC_SECRET=shared_secret_used_to_sign_postbacks
# DIGIPAY_TCOMPLETE_BASE=https://puretide.com
# DigiPay notes: no cancel/failure redirect URL, no automatic postback retries, no transaction ID in postback payload
