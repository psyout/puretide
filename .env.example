# SMTP — Order confirmation & admin notifications
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=orders@yourdomain.com
SMTP_PASS=your_smtp_password
SMTP_FROM=orders@yourdomain.com
SMTP_BCC=
CONTACT_FROM=info@yourdomain.com
ORDER_NOTIFICATION_EMAIL=orders@yourdomain.com

# Low stock alerts (can reuse SMTP or use different)
LOW_STOCK_SMTP_HOST=mail.yourdomain.com
LOW_STOCK_SMTP_PORT=587
LOW_STOCK_SMTP_SECURE=false
LOW_STOCK_SMTP_USER=info@yourdomain.com
LOW_STOCK_SMTP_PASS=your_smtp_password
LOW_STOCK_FROM=info@yourdomain.com
LOW_STOCK_FROM_NAME=Your App Low Stock

# Google Sheets — Products, stock, clients, promos
GOOGLE_SHEET_ID=your_google_sheet_id
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_SHEET_NAME=

# Wrike — Optional: orders & stock alert tasks
WRIKE_API_TOKEN=your_wrike_api_token
WRIKE_ORDERS_FOLDER_ID=your_orders_folder_id
WRIKE_STOCK_ALERTS_FOLDER_ID=your_stock_alerts_folder_id
WRIKE_CLIENTS_FOLDER_ID=your_clients_folder_id

# DigiPay — Credit card payments (required for card checkout)
DIGIPAY_SITE_ID=your_site_id
DIGIPAY_ENCRYPTION_KEY=your_encryption_key
DIGIPAY_POSTBACK_URL=https://yourdomain.com/api/digipay/postback
DIGIPAY_TCOMPLETE_BASE=https://yourdomain.com
# DigiPay postback sender IPs (they send callbacks from these). 88.221.139.21 added by DigiPay. For production omit 127.0.0.1,::1.
DIGIPAY_POSTBACK_ALLOWED_IP=185.240.29.227,45.225.185.23,88.221.139.21,127.0.0.1,::1

# DigiPay sandbox (testing bed). Sandbox site ID = 6101. Test session = 6101_test.
DIGIPAY_SANDBOX_SITE_ID=6101
# Set to true to use sandbox site for payment redirects (full flow tests)
DIGIPAY_USE_SANDBOX=true

# DigiPay HMAC — DigiPay confirmed they do NOT send signature headers.
# Keep UNSET (leave out or comment) or postbacks will fail with error 103.
# DIGIPAY_POSTBACK_HMAC_SECRET=

# Dashboard — Sign in at /dashboard/login to use orders/stock without API keys
# When set, /dashboard/* is protected; use /api/dashboard/orders and /api/dashboard/stock from the dashboard.
# DASHBOARD_SECRET=set_a_strong_secret_here

# API keys for protected routes (set to enable; optional if using dashboard login)
# ORDERS_API_KEY=
# STOCK_API_KEY=
# PROMO_API_KEY=   — GET/POST /api/promo when set (dashboard uses /api/dashboard/promo)
# CLIENTS_API_KEY= — GET /api/clients when set (dashboard uses /api/dashboard/clients)
# CRON_SECRET=     — POST /api/stock/alert (cron); send as x-cron-secret or Bearer

# Deploy (deploy.sh) — set in environment; do not commit production values
# VPS_HOST=your-server.com
# VPS_USER=root
# VPS_PATH=/var/www/puretide
# PM2_APP=puretide
